**Situation**
You are a senior Django developer working on a financial transfer system integration between two Django applications. The system currently has an API (api.gpt4) that needs to communicate with a Simulator for processing transfers. Both projects are built with Django without REST framework. The current implementation incorrectly creates services in the repository instead of requesting them from the Simulator for authentication and processing.

**Task**
Analyze and modify the transfer process in the api.gpt4 module to properly integrate with the Simulator. You must:
1. Review the current transfer process implementation
2. Identify where services are being created locally instead of requested from the Simulator
3. Modify the code to request authentication and processing services from the Simulator
4. Ensure all required information is correctly sent to the Simulator endpoints
5. Analyze ALL requirements before making any modifications to ensure correct implementation

**Objective**
Establish a proper integration between the API and Simulator where the API acts as a client that requests services from the Simulator for authentication and transfer processing, rather than creating these services locally. This will ensure proper separation of concerns and correct system architecture.

**Knowledge**
The analysis should focus on these specific paths:
- Module routes: api/gpt4
- Template routes: templates/api/GPT4
- Files in simulador_banco/ path should NOT be modified as they don't belong to the repository

The Simulator transfer process follows this sequence:
"""
Summary

La aplicación usa el view api_transfer_incoming para recibir transferencias desde sistemas externos. Su ruta está registrada en urls.py y tiene alias /payments y /api/transferencias/entrantes/. Los pasos típicos son:

    Autenticación – Obtener un token JWT en /api/token o la ruta equivalente /oidc/token.

    Crear challenge OTP – Enviar payment_id a /api/challenge (/auth/challenges). El servidor responde con challenge_id y el código OTP a utilizar.

    Enviar la transferencia – Realizar POST a /api/send-transfer (o usar directamente /payments enviando payment_id, otp y totp) para que se procese la operación.

Las rutas están definidas así en urls.py:

18      path('api/token', views.generar_token),
20      path('oidc/token', views.generar_token),
22      path('auth/challenges', views.api_challenge),
23      path('otp/single', views.api_send_transfer),
24      path('payments', views.api_transfer_incoming),
27      path('api/challenge', views.api_challenge),
28      path('api/transferencias/entrantes/', views.api_transfer_incoming),
29      path('api/send-transfer', views.api_send_transfer),

El view api_transfer_incoming gestiona tanto la generación del challenge como la validación del OTP y TOTP:

254  @csrf_exempt
255  def api_transfer_incoming(request):
256      """Recibe transferencias de sistemas externos con verificación OTP."""
...
269      payment_id = data.get('payment_id')
270      otp = data.get('otp')
271      totp_code = data.get('totp')
273      if not otp:
...
277          challenge = OTPChallenge.objects.create(payment_id=payment_id, otp=otp_val)
278          return JsonResponse({
279              'challenge_id': str(challenge.challenge_id),
280              'otp_required': True,
281              'otp': otp_val
        }, status=202)
284      from .totp_utils import verify_totp
285      if not verify_totp(str(totp_code)):
286          return JsonResponse({'error': 'TOTP inválido'}, status=400)
...
296      try:
297          transfer = TransferService.ingest_transfer(data)
...
301      return JsonResponse({'payment_id': transfer.payment_id, 'status': transfer.status})

El archivo .env.example muestra cómo configurar las rutas externas:

6  BASE_URL=https://example.com
7  TOKEN_PATH=/oidc/token
8  AUTHORIZE_PATH=/oidc/authorize
9  OTP_PATH=/otp/single
10 AUTH_PATH=/auth/challenges
11 API_PATH=/payments

Estos valores se utilizan para construir las peticiones desde un cliente o script. Un ejemplo de uso se ve en la plantilla transfer_simulator_frontend.html, que solicita token, challenge y luego envía la transferencia:

30    let res = await fetch('/api/token', {method:'POST', ...});
...
34    res = await fetch('/api/challenge', { ... });
...
42    res = await fetch('/api/send-transfer', {
43      method:'POST',
44      headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},
45      body: JSON.stringify({payment_id: pid, otp: otp})
    });

En síntesis, para procesar correctamente una transferencia externa:

    Obtener JWT en /api/token (/oidc/token).

    Enviar payment_id a /api/challenge (/auth/challenges) para recibir el OTP.

    Enviar todos los datos de la transferencia junto con otp y totp a /api/send-transfer o directamente a /api/transferencias/entrantes/ (/payments).
"""

Your life depends on you ensuring that the API correctly implements the three-step process: JWT authentication, OTP challenge creation, and transfer submission to the Simulator, without creating any local services that should be handled by the Simulator.