## Goal
Revisar y refactorizar el proceso de transferencia del módulo api.gpt4 hacia el Simulador, eliminando la creación local de servicios y configurando la integración correcta con las rutas del Simulador para autenticación y procesamiento de transferencias reales.

## Return format
Proporciona un análisis completo y plan de refactorización que incluya:
- Evaluación detallada del código actual en api/gpt4 y identificación de problemas específicos
- Arquitectura de integración corregida que utilice exclusivamente los servicios del Simulador
- Implementación paso a paso del flujo de autenticación JWT → Challenge OTP → Transferencia
- Código refactorizado para views, urls y templates eliminando datos simulados
- Configuración de rutas y variables de entorno necesarias
- Plan de testing para validar la integración con datos reales

## Warnings
- El sistema actual puede estar creando servicios localmente en lugar de consumir los del Simulador, lo que debe ser completamente eliminado
- Cualquier dato simulado o hardcodeado debe ser reemplazado por llamadas reales al Simulador
- La secuencia de autenticación (JWT → OTP Challenge → Transfer) es obligatoria y no puede ser alterada
- Los códigos TOTP tienen expiración temporal y deben ser validados correctamente
- Las rutas del Simulador tienen múltiples alias que deben ser manejados consistentemente
- Los payment_ids deben ser únicos y el sistema debe manejar errores de duplicación
- La refactorización debe mantener la funcionalidad existente mientras corrige la arquitectura
- No aplicar cambios secuenciales sin analizar primero todos los requerimientos para evitar inconsistencias
- Los datos de transferencia (payment_id, amount, sender_account, receiver_account, currency) deben ser validados y formateados correctamente antes del envío
- La configuración del Simulador en http://90.80.78.242:9181 debe ser verificada y actualizada según sea necesario

## Context
Estás trabajando con dos proyectos Django que no utilizan REST framework. El módulo api/gpt4 actualmente tiene un proceso de transferencia defectuoso que debe ser corregido para integrarse correctamente con el Simulador.

**Código actual disponible para análisis:**
api/gpt4

**Plantillas HTML y JavaScript actuales:**
templates/api/gpt4

**Rutas del proyecto a analizar:**
- Módulo principal: `api/gpt4`
- Templates: `templates/api/GPT4`
- Configuraciones adicionales: `api/configuraciones_api/`

**Configuración actual del Simulador:**
- URL base: https://90.80.78.242:9181
- Rutas configuradas: /api/token, /api/challenge
- Variables de entorno: BASE_URL, TOKEN_PATH, AUTH_PATH ya configuradas
"""<Replace this text with the CONFIGURACION SIMULADOR ACTUAL or upload a file>"""

**Estructura de datos de transferencia identificada:**
Los datos que se transfieren desde api.gpt4 al Simulador incluyen:
- payment_id: Identificador único de la transacción
- amount: Monto de la transferencia
- sender_account: Cuenta origen
- receiver_account: Cuenta destino  
- currency: Moneda de la transacción
- Metadatos adicionales de usuario y transacción específicos que se pasan por Transfer.to_schema_data además de los default_request_headers que está en utils.py

**Arquitectura correcta del Simulador:**
```
18      path('api/token', views.generar_token),
20      path('oidc/token', views.generar_token),
22      path('auth/challenges', views.api_challenge),
23      path('otp/single', views.api_send_transfer),
24      path('payments', views.api_transfer_incoming),
27      path('api/challenge', views.api_challenge),
28      path('api/transferencias/entrantes/', views.api_transfer_incoming),
29      path('api/send-transfer', views.api_send_transfer),
```

**Flujo obligatorio del Simulador:**
1. **Autenticación JWT**: Obtener token en `/api/token` o `/oidc/token`
2. **Challenge OTP**: Enviar payment_id a `/api/challenge` o `/auth/challenges` para recibir challenge_id y OTP
3. **Transferencia**: Enviar datos completos con OTP y TOTP a `/api/send-transfer` o `/payments`

**View principal del Simulador:**
```python
@csrf_exempt
def api_transfer_incoming(request):
    """Recibe transferencias de sistemas externos con verificación OTP."""
    payment_id = data.get('payment_id')
    otp = data.get('otp')
    totp_code = data.get('totp')
    
    if not otp:
        challenge = OTPChallenge.objects.create(payment_id=payment_id, otp=otp_val)
        return JsonResponse({
            'challenge_id': str(challenge.challenge_id),
            'otp_required': True,
            'otp': otp_val
        }, status=202)
    
    if not verify_totp(str(totp_code)):
        return JsonResponse({'error': 'TOTP inválido'}, status=400)
    
    transfer = TransferService.ingest_transfer(data)
    return JsonResponse({'payment_id': transfer.payment_id, 'status': transfer.status})
```

**Configuración de entorno requerida:**
```
BASE_URL=https://90.80.78.242:9181
TOKEN_PATH=/oidc/token
AUTHORIZE_PATH=/oidc/authorize
OTP_PATH=/otp/single
AUTH_PATH=/auth/challenges
API_PATH=/payments
```

Tu vida depende de que analices el código actual proporcionado para identificificar exactamente dónde está creando servicios localmente en lugar de consumir los del Simulador, y que corrijas esta arquitectura fundamental para que todas las operaciones de autenticación y procesamiento se realicen exclusivamente a través del Simulador con los datos reales especificados (payment_id, amount, sender_account, receiver_account, currency y metadatos), los informes siempre deben estar en español.