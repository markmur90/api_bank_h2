#!/bin/sh

LOG_DIR="$HOME/logs"
LOG_FILE="$LOG_DIR/post_compile.log"
DESTINO="$HOME/schemas/keys/ecdsa_private_key.pem"

mkdir -p "$(dirname "$DESTINO")"
mkdir -p "$LOG_DIR"

echo "🔐 Restaurando clave privada desde PRIVATE_KEY_B64..." | tee -a "$LOG_FILE"

STATUS="OK"

if [ -n "$PRIVATE_KEY_B64" ]; then
    echo "$PRIVATE_KEY_B64" | base64 -d > "$DESTINO" 2>>"$LOG_FILE"
    if [ $? -eq 0 ]; then
        chmod 600 "$DESTINO"
        if grep -q "BEGIN PRIVATE KEY" "$DESTINO"; then
            echo "✅ Clave restaurada correctamente en $DESTINO" | tee -a "$LOG_FILE"
        else
            echo "❌ Clave restaurada no válida (no contiene BEGIN PRIVATE KEY). Eliminando." | tee -a "$LOG_FILE"
            rm -f "$DESTINO"
            STATUS="INVALID"
        fi
    else
        echo "❌ Error al decodificar PRIVATE_KEY_B64. Clave no restaurada." | tee -a "$LOG_FILE"
        rm -f "$DESTINO"
        STATUS="DECODE_ERROR"
    fi
else
    echo "⚠️ PRIVATE_KEY_B64 no está definido. Clave no restaurada." | tee -a "$LOG_FILE"key pe
    STATUS="UNDEFINED"
fi

# 🔍 Resultado final resumen
echo ""
case "$STATUS" in
    "OK")
        echo "🔒 Resultado post_compile: ✅ CLAVE OK" | tee -a "$LOG_FILE"
        ;;
    "INVALID")
        echo "🔒 Resultado post_compile: ❌ CLAVE INVÁLIDA (no es PEM)" | tee -a "$LOG_FILE"
        ;;
    "DECODE_ERROR")
        echo "🔒 Resultado post_compile: ❌ ERROR EN DECODIFICACIÓN" | tee -a "$LOG_FILE"
        ;;
    "UNDEFINED")
        echo "🔒 Resultado post_compile: ⚠️ VARIABLE PRIVATE_KEY_B64 NO DEFINIDA" | tee -a "$LOG_FILE"
        ;;
esac

echo ""
